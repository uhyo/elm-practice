{
  "name": "elm-practice",
  "version": "1.0.0",
  "description": "",
  "main": "elm.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && elm make src/Main.elm --optimize --output=dist/elm.js",
    "server": "elm-live src/Main.elm -- --output=elm.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "uhyo",
  "license": "MIT",
  "devDependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-live": "^3.4.1"
  }
}
